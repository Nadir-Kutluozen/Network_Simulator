package org.example.network_simulator;

public class Router extends NetworkDevice {
    public Router(double x, double y) {
        super("Router", x, y);
    }
    // Router specific properties if needed
}